# Database Configuration
spring.datasource.url=jdbc:mysql://localhost:3306/grant_management
spring.datasource.username=root
spring.datasource.password=root
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JPA Configuration
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL8Dialect

# Server Configuration
server.port=8080
server.address=0.0.0.0

# Logging Configuration
logging.level.org.springframework=INFO
logging.level.com.grantmanagement=DEBUG

# File Storage Configuration
file.upload-dir=/home/ubuntu/grant-management-uploads

# JWT Configuration
jwt.secret=yourSecretKey
jwt.expiration=86400000

# Digital Signature Configuration
digital.signature.keystore.file=/home/ubuntu/grant-management-keystore.p12
digital.signature.keystore.password=changeit
digital.signature.key.alias=grantmanagement
digital.signature.key.password=changeit

# Enable CORS
spring.webmvc.cors.allowed-origins=*
spring.webmvc.cors.allowed-methods=GET,POST,PUT,DELETE,OPTIONS
spring.webmvc.cors.allowed-headers=*

# Email Configuration
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=grantmanagement@example.com
spring.mail.password=placeholder_password
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
